package ru.xtool.tests;// Generated by Selenium IDE
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
public class SeoTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    driver = new ChromeDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  public String waitForWindow(int timeout) {
    try {
      Thread.sleep(timeout);
    } catch (InterruptedException e) {
      e.printStackTrace();
    }
    Set<String> whNow = driver.getWindowHandles();
    Set<String> whThen = (Set<String>) vars.get("window_handles");
    if (whNow.size() > whThen.size()) {
      whNow.removeAll(whThen);
    }
    return whNow.iterator().next();
  }
  @Test
  public void seo() {
    driver.get("https://xtool.ru/");
    driver.manage().window().setSize(new Dimension(1370, 786));
    driver.findElement(By.cssSelector(".list-group-item:nth-child(6)")).click();
    driver.findElement(By.id("url")).click();
    driver.findElement(By.id("url")).sendKeys("github.com");
    driver.findElement(By.id("check-submit")).click();
    driver.findElement(By.id("url")).click();
    driver.findElement(By.cssSelector(".center:nth-child(3)")).click();
    driver.findElement(By.id("url")).sendKeys("vk.com");
    driver.findElement(By.cssSelector(".audit")).click();
    vars.put("window_handles", driver.getWindowHandles());
    driver.findElement(By.cssSelector(".trust:nth-child(1)")).click();
    vars.put("win6490", waitForWindow(2000));
    assertThat(driver.switchTo().alert().getText(), is("Введите YES для подтверждения перехода на сайт https://vk.com"));
    driver.findElement(By.linkText("Проверка ИКС")).click();
    driver.findElement(By.id("url")).click();
    driver.findElement(By.id("url")).sendKeys("https://github.com/");
    driver.findElement(By.id("check-submit")).click();
    js.executeScript("window.scrollTo(0,332)");
    driver.findElement(By.cssSelector(".trust")).click();
    driver.findElement(By.cssSelector(".trust")).click();
    driver.findElement(By.id("url")).click();
    driver.findElement(By.id("analyze-form")).click();
    {
      WebElement element = driver.findElement(By.id("analyze-form"));
      Actions builder = new Actions(driver);
      builder.doubleClick(element).perform();
    }
    driver.findElement(By.id("url")).sendKeys("vk.com");
    driver.findElement(By.cssSelector(".audit")).click();
    {
      Alert alert = driver.switchTo().alert();
      alert.sendKeys("YES");
      alert.accept();
    }
    vars.put("root", driver.getWindowHandle());
    driver.switchTo().window(vars.get("win6490").toString());
    driver.switchTo().window(vars.get("root").toString());
  }
}
